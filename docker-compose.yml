services:
  gitlab-mcp:
    build: .
    environment:
      - GITLAB_TOKEN=${GITLAB_TOKEN}
      - GITLAB_PROJECT_ID=${GITLAB_PROJECT_ID}
      - GITLAB_URL=${GITLAB_URL:-https://gitlab.com}
    stdin_open: true
    tty: true
